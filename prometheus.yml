global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: "system-monitor"
    static_configs:
      - targets: ["system-monitor:3000"]
    metrics_path: "/metrics"
    scrape_interval: 10s
